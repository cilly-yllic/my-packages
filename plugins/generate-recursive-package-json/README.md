# generate-recursive-package-json

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build generate-recursive-package-json` to build the library.

## Running unit tests

Run `nx test generate-recursive-package-json` to execute the unit tests via [Vitest](https://vitest.dev/).
